/**
 * Bump only when the standalone relay runtime, its wire contract, or its
 * persisted authorization contract requires the relay container to be
 * replaced independently from the Gateway application.
 */
export const RELAY_VERSION = '1';
