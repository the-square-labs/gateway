{
  "name": "update-service",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "packages/update-service/src",
  "projectType": "application",
  "targets": {
    "build": {
      "executor": "nx:run-commands",
      "options": {
        "command": "pnpm --filter @sqgateway/update-service build"
      }
    },
    "test": {
      "executor": "nx:run-commands",
      "options": {
        "command": "pnpm --filter @sqgateway/update-service test"
      }
    },
    "lint": {
      "executor": "nx:run-commands",
      "options": {
        "command": "pnpm --filter @sqgateway/update-service lint"
      }
    },
    "typecheck": {
      "executor": "nx:run-commands",
      "options": {
        "command": "pnpm --filter @sqgateway/update-service typecheck"
      }
    }
  },
  "tags": []
}
