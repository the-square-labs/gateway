export type DockerDeployMode = "container" | "deployment";

export type DockerDeploySourceMode = "image" | "repository";

export type DockerRestartPolicy = "no" | "always" | "unless-stopped" | "on-failure";
